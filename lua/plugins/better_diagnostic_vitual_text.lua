return {
    lazy = true,
    'sontungexpt/better-diagnostic-virtual-text',
    -- "LspAttach",
    -- config = function(_)
    --     require('better-diagnostic-virtual-text').setup(opts)
    -- end
}
